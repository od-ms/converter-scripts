fahrradverleihe_url = ""

# config for eco-counter-api-reader.py
eco_counter_token = "token"
eco_counter_api_url = "url without trailing slash"
eco_counter_ids = [1,2,3] # Eco counter siteIDs that should be exported

# config für aasee
datahub_api_url = "https://{}/api/device/{}/packets?auth={}"

# github token
github_token = "12345"

